pre-commit:
  parallel: true
  commands:
    lint:
      glob: '*.{js,ts,jsx,tsx}'
      run: bun run lint {staged_files}
    format:
      glob: '*.{js,ts,jsx,tsx,html,md,json,yaml,yml,css,scss}'
      run: bun run format {staged_files}
      stage_fixed: true
